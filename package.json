{
  "name": "training-root",
  "version": "0.0.1",
  "private": true,
  "description": "Root package for commitlint and husky only. The dashboard is a standalone app in /dashboard.",
  "scripts": {
    "prepare": "husky",
    "lint:commits": "commitlint --from HEAD~1 --to HEAD"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
